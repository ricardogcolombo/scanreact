import React from 'react';
import ListItem from './ListItem'

const ListItem = ({
    items
}) => {
    return (
        <div>
        items.map(<ListItem />)
        </div>
    )
}

export default List;
